

name: viennacl

inc: inc
lib: viennacl
path: lib

property:
    org.boost.version: master

profile:
  - name: boost
    dep:
      - name: org.boost
        version: ${org.boost.version}
        profile: chrono date_time serialization thread
